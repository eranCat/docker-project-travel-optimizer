services:
  aco-api:
    build: ./aco-api
    ports:
      - "8000:8000"
    networks:
      - aco-net

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    networks:
      - aco-net
    depends_on:
      - aco-api

networks:
  aco-net:
    driver: bridge